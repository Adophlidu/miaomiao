# Conventions

> Generated by /d:init. Update via `/d:init` (refresh) or edit manually.

## Naming

- **Files:** kebab-case for everything — `sign-in-form.tsx`, `auth-client.ts`, `mode-toggle.tsx` (inferred, grep-verified: 0 PascalCase filenames).
- **React components:** PascalCase identifiers (`SignInForm`, `RootComponent`); the file is kebab-case (inferred).
- **Variables / functions:** camelCase (`createContext`, `todoRouter`, `handleAddTodo`) (inferred).
- **Types / interfaces:** PascalCase (`AppRouter`, `Context`, `RouterAppContext`) (inferred).
- **DB tables & columns:** singular snake_case columns, singular table name — `pgTable("todo", { email_verified... })` exposed as camelCase fields (`emailVerified`) (inferred, `packages/db/src/schema`).
- **Workspace packages:** scoped `@miaomiao/<name>`; apps are unscoped (`web`, `server`) (`package.json`).
- **Path alias:** `@/*` → `apps/web/src/*` in the web app (13 usages); cross-package imports use `@miaomiao/*` (inferred).
- **Branches:** trunk = `main`; `/d:task` → `d/task/<NNNN-slug>`, `/d:fix` → `d/fix/<slug>` (see Commit & PR).

## Structure & Layout

- **Monorepo:** pnpm workspaces (`apps/*`, `packages/*`) with a shared dependency **catalog** in `pnpm-workspace.yaml` — reference shared dep versions as `"catalog:"`, do not hardcode versions (`package.json`).
- **Layer boundaries:** the web client imports `AppRouter` from `@miaomiao/api` **as a type only**; never import server/db code into `apps/web` (`apps/web/src/utils/trpc.ts`).
- **Schema is centralized:** all Drizzle tables live in `packages/db/src/schema/*` and are re-exported from `schema/index.ts` (barrel). The auth package and routers import from there (`inferred`).
- **tRPC routers:** one file per feature under `packages/api/src/routers/`, merged into `appRouter` in `routers/index.ts` (`inferred`; matches tRPC merging-routers guidance, framework default).
- **Imports:** external packages first, then internal (`@miaomiao/*`, `@/*`), then relative — separated by blank lines (inferred). Biome `organizeImports` assist is **off**; keep the existing grouping manually.
- **Exports:** **named exports** are the default (43 named decls). **Default exports** are reserved for React feature/page components in `apps/web/src/components` and for tool config files (`vite.config.ts`, `drizzle.config.ts`) (inferred, grep-verified).

## TypeScript / Language

Strict config in `packages/config/tsconfig.base.json` (extended by every workspace):

- `strict: true`, `noUncheckedIndexedAccess: true`, `noUnusedLocals: true`, `noUnusedParameters: true`, `noFallthroughCasesInSwitch: true` (tsconfig).
- `verbatimModuleSyntax: true` → **type-only imports must use `import type`** (Biome `useImportType` enforces this) (tsconfig + biome).
- `module`/`moduleResolution`: `ESNext` / `bundler`; `isolatedModules: true`; ESM only (`type: module`) (tsconfig).
- Avoid `any` — Biome `noExplicitAny` is on; use `unknown` + narrowing or a precise cast (inferred from the `env/web.ts` fix).
- Validate all external input with **Zod v4** (tRPC `.input(z.object(...))`, env schemas) (inferred).

## Error Handling

- **API layer:** throw `TRPCError` with a `code` (e.g. `"UNAUTHORIZED"`) from procedures; `protectedProcedure` centralizes the auth check (`packages/api/src/index.ts`) (framework default, already adopted).
- **Async:** `async/await` throughout; no `.then()` chains in app code (inferred).
- **Client:** errors surface via a global `QueryCache.onError` → `sonner` toast; per-call `onError` handlers add context (`apps/web/src/utils/trpc.ts`) (inferred).
- **Env:** invalid env fails fast at startup via `@t3-oss/env-core` (Zod) (`packages/env`).

## Testing

- **No test framework is configured yet** — there is no `test` script and no test files (verified). `testGate.test` is `null` in the manifest.
- When adding tests, **Vitest** is the conventional choice for this Vite/TS stack (framework default). Place unit tests next to source as `*.test.ts(x)`; register a `test` script per workspace and re-run `/d:init` to wire the test gate.

## Formatting & Linting

Tooling **authored by /d:init** (none existed) — see "Authored Config" below.

- **Biome** (`biome.json`) is the single lint+format tool.
- Formatter: 2-space indent, `lineWidth` 100, **double quotes**, semicolons always, trailing commas all (matches the scaffold style).
- Linter: `recommended` rules on, with `a11y/noLabelWithoutControl` disabled (false-positive on the reusable shadcn `Label` primitive).
- Excluded from Biome: `**/routeTree.gen.ts`, `**/*.gen.ts`, `**/dist`, and `**/*.css` (Tailwind v4 at-rule syntax the CSS parser rejects).
- CSS is **not** formatted by Biome — Tailwind v4 directives (`@theme`, `@apply`, `@custom-variant`) in `packages/ui/src/styles/globals.css`.

## Commit & PR Conventions

- **Commit format:** [Conventional Commits](https://www.conventionalcommits.org/) — `type(scope): subject`. Allowed types: `feat | fix | docs | refactor | test | chore | perf | build | ci` (default; matches the scaffold commit `chore: ...`). Light-touch — no commitlint config or git hook.
- **PR title:** Conventional-style. **PR body:** `## Summary` / `## Changes` / `## Test Plan`.
- **Branch discipline:** trunk = **`main`**. **Never commit directly to `main`** — all changes land via a PR.
  - `/d:task` works on `d/task/<NNNN-slug>` (spec number + slug).
  - `/d:fix` works on `d/fix/<slug>`.
  - A run finishes by opening a PR into `main` (or pushing + printing PR instructions when a PR can't be created).
- Every `d-*` agent that auto-commits MUST follow this section.

## Known Pitfalls

- `routeTree.gen.ts` is **generated** by the TanStack Router Vite plugin — never hand-edit; it is gitignored and Biome-ignored (`apps/web`).
- `apps/web` `check-types` runs a full `vite build` then `tsc --noEmit` — it is slower than a bare typecheck; expect ~a few seconds.
- The example `todoRouter` uses `publicProcedure` and is **not** user-scoped. Real 记账 data must use `protectedProcedure` and filter by `ctx.session.user.id` — do not copy the public access pattern for user data.
- Catalog versions: bump shared deps in `pnpm-workspace.yaml` `catalog:`, not in individual `package.json` files.

## Authored Config (added by /d:init)

No lint or format tooling existed in the scaffold. /d:init added:

- `biome.json` — Biome v2.5 lint + format config (settings above).
- Root `package.json` scripts: `lint` (`biome lint .`), `format` (`biome format .`), `check` (`biome check .`).
- `@biomejs/biome` dev dependency at the workspace root.

Remove this section's "authored" caveat once the team has adopted Biome as their own.
